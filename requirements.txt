# Full desktop dependency set (Windows/Linux/macOS). Everything in
# requirements-core.txt, plus the three packages that cannot install
# natively on Android/Termux:
#   - chromadb   (semantic memory/skill search -- SQLite keyword-ranking
#                 fallback is used automatically when absent, see
#                 backend/search/)
#   - playwright (browser automation -- unavailable on Android until a
#                 device-driven backend exists, see
#                 backend/browser/android_backend.py)
#   - psutil     (CPU/RAM resource metrics -- degrade to None when absent,
#                 see backend/monitoring/resources.py)
#
# On Android/Termux, install requirements-core.txt instead:
#   pip install -r requirements-core.txt
# `./scripts/install.sh` detects Termux automatically and picks the right
# file for you. See the README's "Android / Termux" section for details.
-r requirements-core.txt
chromadb==0.5.15
playwright==1.47.0
psutil==6.0.0
